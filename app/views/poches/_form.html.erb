<%= form_with(model: poch) do |form| %>
  <% if poch.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(poch.errors.count, "error") %> prohibited this poch from being saved:</h2>

      <ul>
        <% poch.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.text_field :project_id, type: :hidden %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
